<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to 'Cards', root_path, class:"navbar-brand mb-3" %>
  <div class="collapse navbar-collapse" id="navbarColor02">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>

        <li class="nav-item">
          <p class="text-white">Logged in as : <strong class="text-white"><%= current_user.email %></strong>
            <%= image_tag(current_user.image.url, alt: 'Image', class: "rounded-circle", width: "40px", height: "40px") %> 
            <%= link_to "Edit Profile", edit_user_registration_path, :class=>"btn btn-outline-success ml-5"%>
          </p>
        </li>

        <li class="nav-item">
          <%= link_to "Logout", destroy_user_session_path, method: :delete, :class=>"btn btn-outline-danger ml-5"%>
        </li>         
      <% else %>
        <li class="nav-item">
          <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
        </li>

        <li class="nav-item">
          <%= link_to "Sign In", new_user_session_path, class:"nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>